import React from 'react';
import MovieNavBar from '../AppComponents/MovieNavBar';

const MovieHome = () => {
    return (
        <main>
            <MovieNavBar />  
        </main>
    );
};

export default MovieHome;