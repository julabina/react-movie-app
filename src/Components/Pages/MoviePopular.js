import React from 'react';
import MovieNavBar from '../AppComponents/MovieNavBar';

const MoviePopular = () => {
    return (
        <main>
            <MovieNavBar />
        </main>
    );
};

export default MoviePopular;