import React from 'react'; 
import NavBar from '../AppComponents/NavBar';

const Header = (props) => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

export default Header;